"use strict";

// Generated by CoffeeScript 2.5.1
(function () {
  var Discord;
  Discord = require('discord.js');

  module.exports.run = function (client, msg, args) {
    var a, sayMessage;
    sayMessage = args.join(' ');

    if (args.length === 0) {
      msg.channel.send("Voc\xEA \xE9 o culpado ".concat(msg.author) + sayMessage);
      return;
    } else if (args.length > 1) {
      for (a in args) {
        if (!a.startsWith('<@')) {
          msg.channel.send('Comando Inválido');
          return;
        }

        msg.channel.send('Vocês são os culpados ' + sayMessage);
        return;
      }
    } else {
      if (msg.mentions.users.first === client.user.id || msg.mentions.roles.first) {
        msg.channel.send("Eu sou a culpada? Acho que n\xE3o hein. Voc\xEA \xE9 o culpado ".concat(msg.author));
      }
    }

    return msg.channel.send('Você é o culpado ' + sayMessage);
  };
}).call(void 0);